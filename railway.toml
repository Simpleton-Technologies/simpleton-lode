[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server/lode-server.js"
healthcheckPath = "/api/market/XAUUSD"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
