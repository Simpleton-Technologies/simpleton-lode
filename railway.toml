[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server/lode-server.js"
healthcheckPath = "/api/founder/pulse"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
